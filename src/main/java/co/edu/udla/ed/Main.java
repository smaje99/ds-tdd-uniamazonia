package co.edu.udla.ed;

/**
 * Minimal entry point for local manual runs.
 *
 * <p>
 * The repository is primarily exercised through unit tests and classroom
 * exercises, so this class only provides a simple executable target that can be
 * replaced with ad-hoc experiments when needed.
 * </p>
 */
public class Main {

    /**
     * Launches the placeholder application entry point.
     *
     * @param args command-line arguments passed by the runtime
     */
    public static void main(String[] args) {
        System.out.println("Hello world!");
    }
}
